/**
 * Address domain model.
 *
 * The API contract lives in `typescript/schemas/address.schema.ts`; this module
 * is the single import surface the dashboard/checkout UIs use.
 */
export type { Address, Location } from '@/typescript/schemas/address.schema';
